const { fetchWithRetry } = require("./http");

const GEMINI_API_URL =
  "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent";

async function callGemini(apiKey, body) {
  const response = await fetchWithRetry(
    `${GEMINI_API_URL}?key=${apiKey}`,
    {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    },
    { context: "Gemini", timeoutMs: 45000 }
  );

  if (!response.ok) {
    const errText = await response.text();
    throw new Error(`Gemini API error: ${response.status} ${errText}`);
  }

  const data = await response.json();
  const text = data.candidates?.[0]?.content?.parts?.[0]?.text;
  return text ? text.trim() : null;
}

async function rewriteAsFact({ articleText, articleTitle, regionName, themeName, themeLens }) {
  const apiKey = process.env.GEMINI_API_KEY;

  // Ограничиваем длину исходного текста, чтобы не тратить лишние токены
  const trimmedText = articleText.slice(0, 6000);

  const systemPrompt = `Ты — автор популярного Telegram-паблика об истории. Люди листают
ленту за секунду — если первая же фраза не цепляет, пост пролистают не читая.
Ты не Wikipedia и не учебник: никакой справочной, "энциклопедической" интонации.

Тебе дают текст статьи из Wikipedia. Тема выпуска: "${themeName}" (${themeLens}).
Регион: ${regionName}.

ФОРМАТ СТРОГО ТАКОЙ (два блока, разделённые пустой строкой):

[Блок 1 — одно предложение, ХУК]: САМАЯ неожиданная, конкретная деталь или
утверждение из статьи — то, что вызывает реакцию "стоп, что?!". Не общее
описание "что это такое", а сразу самое цепляющее, что там есть.

[пустая строка]

[Блок 2 — одно-два предложения]: контекст и ещё одна конкретная деталь
(цифра, имя, обстоятельство), которая раскрывает или усиливает хук из блока 1.

ПРОВЕРКА САМОГО СЕБЯ: если бы ты рассказал это как байку другу за столом —
он бы удивился и запомнил? Если пост читается как строчка из учебника —
переделай, взяв другую деталь из статьи.

ПРИМЕР нужного стиля (не копируй содержание, только формат):
"Древнеримская мозаика в банях Каракаллы — это не собирательные образы,
а портреты настоящих спортсменов IV века.

На панели больше 100 разных лиц — реальных борцов и боксёров, участников
тогдашних местных соревнований."

Что важно:
- ВСЕГО 2-3 предложения на весь пост. Не больше. Это не абзац, а короткая подпись.
- Делай акцент на теме "${themeName}", если в статье есть на что опереться.
- НЕ выдумывай факты, которых нет в исходном тексте.
- НЕ копируй предложения дословно — формулируй заново, живым разговорным языком.

ЗАПРЕЩЁННЫЙ ТИП ФАКТА — избегай любой ценой:
- Даты, длительности, вычисленные интервалы времени как главный смысл факта
  (например: "X просуществовал 54 года 8 месяцев", "с 1929 по 1941 год...").
  Такие "факты" — это просто арифметика, а не что-то интересное. Дата может
  быть фоном, но НЕ должна быть сутью предложения.
- Общие абстрактные утверждения про "государство", "систему", "эпоху" без
  конкретного физического объекта, человека, числа предметов или детали,
  которую можно представить.
- Пересказ статьи "с начала" (кто, когда основал, чем управлял) — это раздел
  энциклопедии, а не интересный факт. Ищи деталь из середины или конца статьи,
  если начало сухое.

ЧТО ИСКАТЬ ВМЕСТО ЭТОГО — конкретный артефакт, деталь или образ, который
можно представить или увидеть: конкретный предмет, находка, здание,
изображение, число людей/вещей, странная деталь быта, необычное правило
или парадокс. Если статья не даёт такой конкретики — лучше взять другую
деталь из текста, но не заменять её датами/сроками.

- Никакого канцелярита ("колоссальные перемены", "определил облик",
  "грандиозные испытания" и подобных штампов).
- Максимум 1 эмодзи, и то не обязательно.
- Ответь только текстом поста в этом формате, без предисловий от себя.`;

  return callGemini(apiKey, {
    system_instruction: {
      parts: [{ text: systemPrompt }],
    },
    generationConfig: { temperature: 1.15 },
    contents: [
      {
        role: "user",
        parts: [{ text: `Статья "${articleTitle}":\n\n${trimmedText}` }],
      },
    ],
  });
}

// Ранжирует заголовки-кандидаты от самого многообещающего (живая, конкретная
// история) до наименее (сухая "статья-зонтик" про целую страну/эпоху/процесс).
// Используется перед тем, как тратить запросы на загрузку полного текста —
// так реже долетаем до статей вида "Узбекистан" или "Индустриализация СССР".
async function pickMostInteresting(candidates, { regionName, themeName } = {}) {
  if (!candidates || candidates.length <= 1) return candidates || [];

  const apiKey = process.env.GEMINI_API_KEY;

  const systemPrompt = `Ты — редактор популярного Telegram-канала об истории.

Тебе дают список заголовков-кандидатов на сегодняшний пост. Тема выпуска:
"${themeName}". Регион: "${regionName}".

Расставь заголовки по убыванию того, насколько живой и конкретный пост
из них получится.

СТАВЬ В КОНЕЦ списка заголовки, которые почти наверняка дадут сухой
учебниковый текст: названия целых государств, республик, стран, народов,
эпох, идеологий, экономических процессов ("Индустриализация",
"Коллективизация", "Гражданская война", "Узбекистан", "Депрессия (экономика)"),
официальных институтов, договоров, административных реформ.

СТАВЬ В НАЧАЛО списка заголовки, которые звучат как конкретная история:
предмет, находка, здание, человек, случай, произведение, необычное явление,
скандал, техническая деталь — то, что можно живо представить и рассказать
за 2-3 предложения.

Ответь СТРОГО списком заголовков через перенос строки, от самого
многообещающего к наименее, без номеров, пояснений и изменений формулировок.
Верни ВСЕ заголовки из списка, ничего не добавляя.`;

  const text = await callGemini(apiKey, {
    system_instruction: { parts: [{ text: systemPrompt }] },
    generationConfig: { temperature: 0.3 },
    contents: [{ role: "user", parts: [{ text: candidates.join("\n") }] }],
  });

  if (!text) return candidates;

  const ranked = text
    .split("\n")
    .map((s) => s.trim())
    .filter((s) => candidates.includes(s));

  // подстраховка: если модель что-то упустила, дописываем оставшихся
  // кандидатов в конец в исходном порядке, а не теряем их
  const rankedSet = new Set(ranked);
  const missing = candidates.filter((c) => !rankedSet.has(c));
  return [...ranked, ...missing];
}

async function generatePopCultureFact({ topicTitle, topicNote, verified }) {
  const apiKey = process.env.GEMINI_API_KEY;

  const verifiedInstruction = verified
    ? `Это задокументированный факт — пиши уверенно, как установленную информацию.`
    : `Это распространённая интерпретация/толкование, а НЕ официально подтверждённый факт. 
Обязательно формулируй с оговоркой: "многие видят в этом...", "по одной из трактовок...", 
"считается, что..." — НЕ подавай это как безусловную истину.`;

  const systemPrompt = `Ты — автор Telegram-канала об истории, который иногда связывает
исторические/культурные явления с современной поп-культурой (фильмы, игры, музыка).
Пиши так, чтобы человек, листающий ленту, реально удивился связи — а не как
подпись к музейному стенду.

Тема поста: "${topicTitle}" (${topicNote}).

${verifiedInstruction}

ФОРМАТ СТРОГО ТАКОЙ (два блока, разделённые пустой строкой), 2-3 предложения всего:

[Блок 1 — ХУК]: одно предложение — неожиданная, конкретная связь между
произведением и реальным историческим/культурным явлением. Формулируй как
открытие, а не как факт из справочника.

[пустая строка]

[Блок 2]: одно-два предложения — конкретная деталь, которая раскрывает эту связь.

Что важно:
- Никакого канцелярита и общих фраз.
- Пиши конкретно, с именами/деталями, а не абстрактно.
- НЕ выдумывай дополнительных фактов сверх того, что реально известно по теме —
  если сомневаешься в детали, лучше не включать её.
- Максимум 1 эмодзи.
- Ответь только текстом поста, без предисловий от себя.`;

  return callGemini(apiKey, {
    system_instruction: { parts: [{ text: systemPrompt }] },
    generationConfig: { temperature: 1.15 },
    contents: [
      {
        role: "user",
        parts: [{ text: `Напиши пост про "${topicTitle}".` }],
      },
    ],
  });
}

async function rewriteArtifactAsFact({ artifactText, artifactTitle, regionName, themeName, themeLens }) {
  const apiKey = process.env.GEMINI_API_KEY;

  // Ограничиваем длину исходного текста, чтобы не тратить лишние токены
  const trimmedText = artifactText.slice(0, 4000);

  const systemPrompt = `Ты — автор популярного Telegram-паблика об истории. Люди листают
ленту за секунду — если первая же фраза не цепляет, пост пролистают не читая.
Ты не музейная табличка: никакой справочной интонации.

Тебе дают описание музейного экспоната из архива Смитсоновского института (США).
Текст описания на английском — переведи и перескажи его СУТЬ по-русски, дословный
перевод не нужен. Тема выпуска: "${themeName}" (${themeLens}). Регион: ${regionName}.

ФОРМАТ СТРОГО ТАКОЙ (два блока, разделённые пустой строкой):

[Блок 1 — одно предложение, ХУК]: САМАЯ неожиданная, конкретная деталь или
обстоятельство из описания — то, что вызывает реакцию "стоп, что?!". Не общее
описание "что это за предмет", а сразу самое цепляющее, что там есть.

[пустая строка]

[Блок 2 — одно-два предложения]: контекст и ещё одна конкретная деталь из
описания (цифра, имя, обстоятельство), которая раскрывает или усиливает хук.

ПРИМЕР нужного стиля (не копируй содержание, только формат):
"Осколок мраморного камня из закладки монумента Вашингтону, отколотый в 1880-х годах.

Его подобрал коллекционер-любитель, присутствовавший при укреплении фундамента,
и хранил обёрнутым в марлю — как драгоценность."

Что важно:
- ВСЕГО 2-3 предложения на весь пост. Не больше. Это не абзац, а короткая подпись.
- Весь текст СТРОГО на русском языке, даже если источник на английском.
- Делай акцент на теме "${themeName}", если в описании есть на что опереться.
- НЕ выдумывай факты и не добавляй ничего сверх того, что есть в описании.
- НЕ переводи дословно — формулируй заново, живым языком.

ЗАПРЕЩЁННЫЙ ТИП ФАКТА — избегай любой ценой:
- Даты, длительности, вычисленные интервалы времени как главный смысл факта.
  Дата может быть фоном, но НЕ должна быть сутью предложения.
- Общие абстрактные утверждения без конкретного физического объекта, человека,
  числа предметов или детали, которую можно представить.

- Никакого канцелярита ("колоссальные перемены", "определил облик" и подобных штампов).
- Максимум 1 эмодзи, и то не обязательно.
- Ответь только текстом поста в этом формате, без предисловий от себя.`;

  return callGemini(apiKey, {
    system_instruction: {
      parts: [{ text: systemPrompt }],
    },
    generationConfig: { temperature: 1.15 },
    contents: [
      {
        role: "user",
        parts: [{ text: `Экспонат "${artifactTitle}":\n\n${trimmedText}` }],
      },
    ],
  });
}

module.exports = { rewriteAsFact, generatePopCultureFact, rewriteArtifactAsFact, pickMostInteresting };